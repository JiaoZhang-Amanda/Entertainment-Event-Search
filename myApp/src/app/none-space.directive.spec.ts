import { NoneSpaceDirective } from './none-space.directive';

describe('NoneSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new NoneSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
